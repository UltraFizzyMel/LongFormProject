using UnityEngine;
using TMPro;

public class NameEntry : MonoBehaviour
{
    /*public TMP_InputField nameInputField;
    public LeaderboardUpdater leaderboardUpdater;
    public float playerTime;

    public void SubmitName()
    {
        string playerName = nameInputField.text.ToUpper();
        if (playerName.Length > 3)
            playerName = playerName.Substring(0, 3);

        leaderboardUpdater.TryAddNewTime(playerName, playerTime);
        gameObject.SetActive(false); // Hide name entry UI
    }*/
}
